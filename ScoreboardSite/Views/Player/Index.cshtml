@using System.Globalization
@using System.Web.UI.WebControls
@using ScoreboardSite.Models.Scores
@model IEnumerable<ScoreboardSite.ViewModels.AssignedScores>

@{
    ViewBag.Title = "Index";
}

<h2>Scores</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm())
{
    <p>
        @Html.DropDownList("DisCriminators", "Choose Category")
        <input type="submit" value="Filter" />
    </p>
}

<table class="table">
    <tr>
        <th>Player</th>
        <th>Score</th>
    </tr> 

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AccountName)
            </td>
            @if (item.OverallScore != null)
            {
                <td>
                    @Html.DisplayFor(modelItem => item.OverallScore)
                </td>
            }
            @if (item.CompletionTime != null)
            {
                <td>
                    @Html.DisplayFor(modelItem => item.CompletionTime)
                </td>
            }
            @if (item.DeathCount != null)
            {
                <td>
                    @Html.DisplayFor(modelItem => item.DeathCount)
                </td>
            }
            @if (item.MonstersSlayen != null)
            {
                <td>
                    @Html.DisplayFor(modelItem => item.MonstersSlayen)
                </td>
            }
            @if (item.TotalAchievements != null)
            {
                <td>
                    @Html.DisplayFor(modelItem => item.TotalAchievements)
                </td>
            }

            @*<td>
                @Html.DisplayFor(modelItem => item.AccountName)
            </td>*@
            <td>
                
                @*@Html.DisplayFor(modelItem => item.PlayerID)*@
                @{
                    @*foreach (var score in item.Scores)
                    {
                        string switchOn = ViewBag.DisCriminators.SelectedValue;
                        switch (switchOn)
                        {
                            case "OverallScore":
                            {
                                if (score.GetType().Name.Contains("OverallScore"))
                                {
                                    @Html.DisplayFor(modelItem => ((OverallScore)score).TotalScore)

                                }
                                break;
                            }
                            case "CompletionTime":
                            {
                                if (score.GetType().Name.Contains("CompletionTime"))
                                {
                                    @Html.DisplayFor(modelItem => ((CompletionTime)score).TotalTimeToCompletion)
                                }
                                break;
                            }
                            case "DeathCount":
                            {
                                if (score.GetType().Name.Contains("DeathCount"))
                                {
                                    @Html.DisplayFor(modelItem => ((DeathCount)score).TotalDeaths)
                                }
                                break;
                            }
                            case "MonstersSlayen":
                            {
                                if (score.GetType().Name.Contains("MonstersSlayen"))
                                {
                                    @Html.DisplayFor(modelItem => ((MonstersSlayen)score).KillCount)
                                }
                                break;
                            }
                            case "TotalAchievements":
                            {
                                if (score.GetType().Name.Contains("TotalAchievements"))
                                {
                                    @Html.DisplayFor(modelItem => ((TotalAchievements)score).AchievementCount)
                                }
                                break;
                            }
                        }
                    }*@

                }
            </td>
        </tr>
    }

</table>
